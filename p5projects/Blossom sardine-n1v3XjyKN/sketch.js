// wk 02 practice 1
function setup() {
  createCanvas(400, 400);
  //let xpos = 300;
  // xpos doesn't work
  // because it's not used

}

function draw() {
  background(220);
  circle(mouseX, mouseY, 100);
  // fixed mouseX added mouseY
  console.log("iteration")
}