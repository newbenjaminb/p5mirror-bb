// let rx = 0;
let ry = 0;

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  // draw grid of 10x10 rectangles 
  //each rectangle is 40x40
  // x y nested loops
  for(var rx=0, )
  rect(rx,ry,40,40);
}