// challenge colors in array

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}


// challenge put a number of colors in a JavaScript array, and generate a random number to select one of the colors in the array.